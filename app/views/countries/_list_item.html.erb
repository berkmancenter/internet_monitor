<span class="country-name">
    <%= link_to("#{country.name} #{country.score.round(2) unless country.score.nil?}", country) %>
</span>

<span class="gear score" data-score-desc="total-for-<%= country.id %>">
    <%= country.score.round( 2 ) unless country.score.nil? %>
</span>
