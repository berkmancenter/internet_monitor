<% provide :title, @country.name %>


<article class="view" data-country-id="<%= @country.id %>">
  <h1 class="country-1-bg"><%= @country.name %></h1>

  <div class="sub-content country-6-bg">
    <p class="country-description"><%= @country.description %></p>

    <%= render partial: "countries/sidebar" %>
  </div>
</article>
